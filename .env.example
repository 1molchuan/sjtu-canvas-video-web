# Copy to .env only for local operator use; .env is ignored by Git.
SJTU_CANVAS_CONFIG=config/local.toml
# Required by the real protocol server and every real protocol command.
# It is deliberately absent from CI and ordinary Mock tests.
SJTU_REAL_PROTOCOL_TEST=1
RUST_LOG=server=info,canvas_core=info
