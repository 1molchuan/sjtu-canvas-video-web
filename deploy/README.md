# Deployment layer

Cloudflare Tunnel, Caddy/Axum static serving, and the two macOS `launchd` service definitions are
scheduled for Phase 5. No deployment example in Phase 0 should be treated as production-ready.

